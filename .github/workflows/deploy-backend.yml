name: Deploy Backend to EC2

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'apps/be/**' # Chỉ chạy khi có thay đổi trong thư mục code Backend

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    
    steps:
    - name: Kết nối SSH và Deploy Backend
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          cd ~/SMART_RESTAURANT_CBD
          
          # Lưu lại những thay đổi tạm thời nếu có để tránh lỗi git pull
          git stash || true
          
          # Kéo code mới nhất từ nhánh main
          git fetch origin main
          git reset --hard origin/main
          
          # Cài đặt thư viện bằng pnpm từ root (vì đây là monorepo)
          pnpm install --frozen-lockfile
          
          # Chạy database migration (nếu có)
          cd apps/be
          npx knex migrate:latest || true
          
          # Khởi động lại PM2 với Zero-Downtime
          pm2 reload smart-restaurant-backend || pm2 restart smart-restaurant-backend
